// amount, keyboard and mouse, different no. of keyboards with different costs of(n), some no. of mouses(m)
#include<stdio.h>
int main(){
    int m, p;
    int arr[m]; // mouse
    int arr[p]; // hdd
    int amount;
    int k;  //keyboard
    int keyboard[k];
    // logic(m+n+p<=amount)---> buy it, 

    scanf("%d", &amount);
    scanf("%d", keyboard+i);





    for(int i=0;i<d_number;i++)
    scanf("%d", hdd+i);

    for(int i=0;i<k_number;i++)

}